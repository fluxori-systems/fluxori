import '@testing-library/jest-dom';
import '@testing-library/react';

// Add custom types for Testing Library queries or render functions if needed

declare global {
  // Example: type CustomRender = typeof import('@testing-library/react').render;
}

export {};
