/**
 * Scheduled Tasks Module Public API
 *
 * This file defines the public interface of the Scheduled Tasks module, exporting only what should be
 * accessible to other modules. This ensures proper encapsulation and prevents direct access to
 * internal components.
 */

// Re-export module
export { ScheduledTasksModule } from './scheduled-tasks.module';

// Note: This module is currently a stub.
// As the module is developed with services or task definitions, they should be exported here.
