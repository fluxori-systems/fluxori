/**
 * Token for storage service provider
 *
 * Use this token for dependency injection of StorageService
 * instead of using the interface directly.
 */
export const STORAGE_SERVICE = Symbol("STORAGE_SERVICE");
